# Контекст проекта: лендинг «Камилла Тимошенко»

> Снимок состояния на 2026-07-11. Файл для возобновления работы.

## Что это

Одностраничный лендинг врача-диетолога **Камиллы Тимошенко** (к.м.н.), ЦА — женщины 45+,
пери/менопауза. Главная цель: заявка на бесплатный разбор → личное сопровождение (50 000 ₽/мес).

- **Папка проекта:** `/Users/admin/Desktop/dieta/kamilla-site/`
- **Актуальное ТЗ:** `~/Downloads/TZ_site_Kamilla_v2.md` (версия 2.0 с дизайн-системой — главное).
  Старое `~/Downloads/TZ_site_Kamilla_Timoshenko.md` устарело по дизайну, текст частично актуален.

## Текущий дизайн (v4 «Деним и коралл», выбрано Камиллой 2026-07-10)

Холодная джинсово-синяя база + тёплый коралловый акцент. Цвета сняты пипеткой с её фото (одежда).
Палитра выбрана из 3 вариантов (файл `~/Downloads/palitry-kamillaaaa.html`, выбрана «Палитра 1»).
**Задеплоено в эфир** (см. раздел «Деплой»). Имена CSS-переменных сохранены от v3 — просто перекрашены
(`--olive` теперь деним и т.д.), чтобы не трогать разметку.

- Палитра (токены в `css/styles.css` `:root`): `--bg #EEF1F5`, `--bg-alt #F7F9FB`,
  `--olive #3E526E` (деним, осн. бренд), `--olive-deep #232F44` (тёмные секции),
  `--cream-on-dark #D5DFEA`, акцент коралл `--accent #E87D63` (только кнопки/цена/ключевое,
  hover `#C85B42`), `--accent-soft #F3C3AC` (персик, 2-й акцент), текст `#1F2836`,
  muted `#5B6675`, линии `#DCE3EC`, маркер `--mark #F7D8C9`, danger `#C0453B`.
  Плейсхолдеры фото — градиент `#DCE3EC→#C6D2E0`; фокус-ринг формы `rgba(62,82,110,.18)`;
  фон «стеклянной» шапки `rgba(238,241,245,.9)`.
- Шрифты: **Playfair Display** (заголовки) + **Inter** (текст ≥18px).
- Единый ритм секций 96/56px, чередование фонов, 3 тёмные секции-якоря (Энергия, Личка, Финал),
  личка — кульминация (деним+коралл). Signature: `<span class="hl">…</span>` (1 на экран).

## Структура файлов

```
kamilla-site/
├── index.html        — лендинг (секции C0–C14 из ТЗ)
├── css/styles.css    — дизайн-система v3 (токены в :root)
├── js/app.js         — меню, скролл, FAQ, валидация формы, reveal, «наверх»
├── privacy.html, oferta.html, thanks.html — служебные страницы
├── assets/           — фото/PDF (пока плейсхолдеры)
└── README.md
```

## Деплой (GitHub Pages)

Live: **https://hasler777.github.io/vladlenafit-demo/**
Раздаётся из репо `Hasler777/vladlenafit-demo`, ветка `main`, корень `/`.
Рабочая копия этого репо: `/Users/admin/Desktop/dieta/vladlenafit-copy/`.

Процедура выкатки:
```bash
cd /Users/admin/Desktop/dieta/vladlenafit-copy
cp -R ../kamilla-site/. .
git add -A
git -c user.name="Hasler777" -c user.email="a.platya@gmail.com" commit -m "..."
git push origin main          # push бывает медленным — ставить timeout ~300s
# дождаться сборки: gh api repos/Hasler777/vladlenafit-demo/pages/builds/latest → status: built
```

## Версии-резервы (ветки того же репо)

- `kamilla-v1-clean-backup` — первая тёплая версия (шалфей/крем, Lora/Manrope). Отвергнута.
- `kamilla-v2-lime-backup` — версия в стиле vladlenafit (лайм/чёрный, Roboto Condensed). Отвергнута.
- v3 (олива/крем + терракот по ТЗ v2) — отвергнута, заменена на v4. Токены v3: `--bg #F5F1E8`,
  `--olive #6E7B4F`, `--olive-deep #4F5938`, акцент `#C8732E`, маркер `#CBD79B`.
- `main` — текущая v4 «Деним и коралл». Деплой-коммит перекраски: `b5403d2` (репо vladlenafit-demo).
- Оригинал зеркала vladlenafit — коммит `31fa620`.

## Локальный просмотр

```bash
cd /Users/admin/Desktop/dieta/kamilla-site && python3 -m http.server 8300
# http://127.0.0.1:8300/
```
Проверка вёрстки — Chrome headless `--screenshot` (так смотрел все правки).

## Что уже поправлено по ходу

- Шапка: меню наезжало на логотип → порог меню поднят до 1160px, CTA в шапке укорочен до
  «Записаться», логотип `flex:none` (не сжимается).
- Hero-кнопки: текст в одну строку, обе в один ряд на десктопе.
- Карточки боли/метода: заголовки выровнены по высоте (`min-height`), описания на одной линии.
- Плашки-анализы: переверстаны (подпись сверху, крупные цифры снизу), ряды выровнены.

## Ждём от заказчика (плейсхолдеры в коде)

- [ ] Фото Камиллы: hero (справа) + «обо мне» (слева) — есть на руках.
- [ ] Фото кейсов было/стало (6 шт., формат 2:3) + согласия на публикацию — есть на руках.
- [ ] `assets/kniga-blagodarnosti.pdf` — есть у заказчика.
- [ ] Реквизиты ИП (ИНН, ОГРН), цены группы «Солнышки» и мини-продуктов.
- [ ] Тексты политики ПД и оферты (страницы-заглушки готовы).
- [ ] Бэкенд формы (почта/Telegram-бот) — точка вставки в `js/app.js` (сейчас имитация успеха).
- [ ] Яндекс.Метрика: `window.YM_ID = …` (цели form_submit, telegram_click уже шлются).
- [ ] Файлы мини-продуктов (гайд, видео-разбор).

Контакты: личка → `@camilla_tim`, группа/вопросы → `@SVM1986` (Валентина).
